apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: keycloak
  namespace: flux-system
spec:
  interval: 15m
  path: cluster/keycloak
  prune: true
  timeout: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: namespace-keycloak
      namespace: flux-system
    - name: gateway
      namespace: flux-system
    # Keycloak stores its data in Postgres now, so the instance and the Job
    # that creates its role and database have to land first.
    - name: postgres
      namespace: flux-system
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: keycloak
      namespace: keycloak
